angular.module('swapr').controller('ProfileCtrl', ['$scope', 'Profile', function($scope, Profile) {
    //TODO: enable profiles, students currently don't have access to user table
    /*
    Profile.getProfile().success(function(data) {
        $scope.profile = data;
    });
    */
}]);
