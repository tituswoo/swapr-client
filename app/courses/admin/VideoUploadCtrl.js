angular.module('swapr.admin').controller('VideoUploadCtrl', ['$scope', '$stateParams', function ($scope, $stateParams) {
    // @todo: get param id to load in the data for the assignment.
    console.log($scope.currentAssignment);
}]);
